{% extends "__layout.twig" %}

{% block content %}
	<div class="container">
		<div class="row px-0" >
			{% for object in objects %}
				<div class="col-12 col-xs-12 col-sm-8 col-md-4 col-lg-3 mt-1">
					<!--{% if loop.first %}
						first
					{% elseif loop.last %}
						last
					{% else %}
						<div>{{loop.index0}}
							|
							{{loop.length}}
						</div>
					{% endif %}-->

					{% include "menu.html" %}

				</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}
